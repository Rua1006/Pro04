<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="free">
	<select id="freeList" resultType="com.hami.dto.FreeDTO">
		select * from free order by resdate desc
	</select>
	<select id="getFree" resultType="com.hami.dto.FreeDTO">
		select * from free where bno=#{bno}
	</select>
	<insert id="freeInsert">
		insert into free values((select nvl(max(bno), 0)+1 from free), #{title}, #{content}, 'admin', sysdate, 0)
	</insert>
	<delete id="freeDelete">
		delete from free where bno=#{bno}
	</delete>
	<update id="freeEdit">
		update free set title=#{title}, content=#{content} where bno=#{bno}
	</update>
</mapper>